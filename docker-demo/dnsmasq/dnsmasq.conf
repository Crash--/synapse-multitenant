# Resolve every *.localhost to the Traefik container's static IP.
# Traefik holds .253 (outside the default IPAM range, no race with unpinned services).
address=/.localhost/172.28.0.253
no-resolv
log-queries
